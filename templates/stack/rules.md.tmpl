# {{ stack.name }} — Stack Rules

Only include rules that remain true across projects using this stack.

Do not include:

- project domain language;
- project-specific module graphs;
- project-specific endpoints;
- product rules.
