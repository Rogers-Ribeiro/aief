project:
  id: "{{ project.id }}"
  name: "{{ project.name }}"

foundation:
  version: "{{ foundation.version }}"
  conformance: "{{ foundation.conformance }}"

stacks:
{{ stacks_yaml }}

risk_flags:
  enabled: []
  project_specific: []

quality:
  capabilities: {}

governance:
  waivers: ".ai/waivers"
  baseline: null
